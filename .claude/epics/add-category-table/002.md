---
name: Create Category Model
status: closed
phase: 1
priority: P1
complexity: simple
recommended_model: sonnet
parallel: true
prd_requirements: [FR-3]
files: [lib/models/category.dart, lib/models/models.dart]
updated: 2026-03-18T13:00:00Z
---

# Task: Create Category Model

## Context
Define the data structure for categories.

## Description
Create `lib/models/category.dart` with a `Category` class containing `categoryId`, `name`, and `type`. Implement standard `fromMap` and `toMap` methods. Export this model in `lib/models/models.dart`.

## Implementation Steps
- Step 1: Create `lib/models/category.dart`.
- Step 2: Implement the `Category` class with required fields.
- Step 3: Add `Category` export to `lib/models/models.dart`.

## Acceptance Criteria
- [ ] **FR-3 / Model:** `Category` class is defined and usable.
- [ ] **FR-3 / Serialization:** `fromMap` correctly parses database rows.

## Verification
- Run `fvm flutter test` (if unit tests exist for models).
