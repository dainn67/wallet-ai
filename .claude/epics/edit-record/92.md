---
github: https://github.com/dainn67/wallet-ai/issues/92
updated: 2026-03-22T00:00:00Z
name: Update Record Model
status: closed
phase: 1
priority: P1
estimated_effort: 0.5d
complexity: simple
recommended_model: sonnet
files: [lib/models/record.dart]
prd_requirements: [FR-3]
updated: 2026-03-22T00:00:00Z
---
github: https://github.com/dainn67/wallet-ai/issues/92
updated: 2026-03-22T00:00:00Z

# Task: Update Record Model

## Context
The database schema is changing from `created_at` to `last_updated`. The `Record` model must reflect this change.

## Description
Modify `lib/models/record.dart` to replace `createdAt` with `lastUpdated` and update all associated methods.

## Implementation Steps
1. Open `lib/models/record.dart`.
2. Rename the field `createdAt` to `lastUpdated`.
3. Update the constructor: rename the `createdAt` parameter to `lastUpdated`.
4. Update `toMap()`: change key `'created_at'` to `'last_updated'`.
5. Update `fromMap()`: change key `'created_at'` to `'last_updated'`.
6. Update `copyWith()` and `toString()`.

## Acceptance Criteria
- [ ] Field `lastUpdated` exists in `Record` class.
- [ ] `toMap()` outputs `'last_updated'`.
- [ ] `fromMap()` correctly parses `'last_updated'`.
- [ ] App compiles without errors in the model file.
