---
name: Integrate RecordWidget in Chat
status: closed
priority: P1
complexity: simple
recommended_model: sonnet
phase: 2
parallel: true
depends_on: [70]
files: [lib/screens/home/tabs/chat_tab.dart]
prd_requirements: [FR-1, US-1]
github: https://github.com/dainn67/wallet-ai/issues/71
---

# Task: Integrate RecordWidget in Chat

## Context
Now that `RecordWidget` exists, we need to use it in the chat interface to eliminate duplication and ensure a consistent look.

## Description
Replace the inline `_buildRecordCard` in `lib/screens/home/tabs/chat_tab.dart` with the new `RecordWidget`. Remove the redundant logic and verify that the layout remains stable within chat bubbles.

## Interface Contract
- **Receives from T70:** `RecordWidget` component in `lib/components/record_widget.dart`.

## Implementation Steps
1. **Import `lib/components/components.dart`** in `lib/screens/home/tabs/chat_tab.dart`.
2. **Update `ChatBubble` class**:
   - Replace calls to `_buildRecordCard(context, record)` with `RecordWidget(record: record)`.
   - Ensure `RecordWidget` is wrapped in `Flexible` if needed to prevent layout overflows.
3. **Clean up**:
   - Delete the `_buildRecordCard` method from `ChatTab`.

## Acceptance Criteria
- [ ] **FR-1 / Integration:** `ChatBubble` correctly renders `RecordWidget` for all records.
- [ ] **US-1 / Consistency:** Record items in chat look identical to those in the future history view.
- [ ] **NFR-2 / Stability:** No horizontal overflows occur in the chat bubble when rendering record items.

## Verification
1. **Manual Check:** Send messages to the AI that trigger record creation.
2. **Visual Check:** Verify the record cards appear correctly within the assistant's chat bubble.
3. **Overflow Check:** Test with long record descriptions to ensure text wrapping/ellipsis works as intended.
