---
name: Integrate RecordWidget in History
status: closed
priority: P1
complexity: simple
recommended_model: sonnet
phase: 2
parallel: true
depends_on: [70]
files: [lib/screens/home/tabs/records_tab.dart]
prd_requirements: [FR-1, US-1]
github: https://github.com/dainn67/wallet-ai/issues/72
---

# Task: Integrate RecordWidget in History

## Context
With `RecordWidget` available, we must use it in the Records tab to eliminate duplicate UI code and ensure visual consistency across the entire app.

## Description
Replace the inline record list item code in `lib/screens/home/tabs/records_tab.dart` with the new `RecordWidget`. Ensure the history list still looks and behaves correctly.

## Interface Contract
- **Receives from T70:** `RecordWidget` component in `lib/components/record_widget.dart`.

## Implementation Steps
1. **Import `lib/components/components.dart`** in `lib/screens/home/tabs/records_tab.dart`.
2. **Update the `ListView` in `RecordsTab`**:
   - Replace the inline `Container` mapping for `filteredRecords` with a call to `RecordWidget(record: record)`.
3. **Clean up**:
   - Remove any old styles or redundant logic that were used specifically for the old record item display.

## Acceptance Criteria
- [ ] **FR-1 / Integration:** `RecordsTab` correctly renders `RecordWidget` for all records in the list.
- [ ] **US-1 / Consistency:** Record items in the history list look identical to those in chat.
- [ ] **NFR-2 / Robustness:** Handles long lists of records gracefully.

## Verification
1. **Manual Check:** Navigate to the Records tab and verify that all income and expense records are displayed using the new component.
2. **Visual Audit:** Confirm that the look and feel matches the chat tab's record display.
3. **Performance Check:** Ensure smooth scrolling through the record list.
