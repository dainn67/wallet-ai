---
name: Extract Records Tab
status: closed
phase: 2
priority: P1
complexity: simple
recommended_model: sonnet
depends_on: [001]
parallel: true
prd_requirements: [FR-2]
files: [lib/screens/home/tabs/records_tab.dart]
---

# Task: Extract Records Tab

## Context
Move the finance records UI logic from `chat_screen.dart` into its own `RecordsTab` widget.

## Description
Create `lib/screens/home/tabs/records_tab.dart`. This should be a `StatelessWidget` that implements the `_RecordsTabContent` from the original `ChatScreen`. It uses `Consumer<RecordProvider>` to display income and expense summaries and the transaction list.

## Implementation Steps
- Step 1: Create `lib/screens/home/tabs/records_tab.dart`.
- Step 2: Implement `RecordsTab` as a `StatelessWidget`.
- Step 3: Copy the build logic from `_RecordsTabContent` into `RecordsTab.build`.
- Step 4: Ensure all necessary imports (`provider.dart`, `google_fonts.dart`, `models.dart`, `providers.dart`) are added.
- Step 5: Verify that `totalIncome`, `totalExpense`, and the record cards are rendered correctly.

## Acceptance Criteria
- [ ] **FR-2 / Happy path:** Records tab displays income and expense summaries correctly.
- [ ] **FR-2 / Empty state:** Displays the "No records yet" message when the list is empty.
- [ ] **NFR-2 / Logic Parity:** All transaction items match the previous UI layout and logic.

## Interface Contract
- Receives: `HomeScreen` layout via `TabBarView`.
- Produces: `RecordsTab` widget for `HomeScreen`.

## Verification
- Verify the `RecordsTab` displays correctly once integrated.
