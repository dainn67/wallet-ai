---
name: Refactor Database & Seeding
status: closed
phase: 1
priority: P1
complexity: simple
recommended_model: sonnet
prd_requirements: [FR-1, FR-2]
files: [lib/repositories/record_repository.dart]
updated: 2024-03-21
---

# Task: Refactor Database & Seeding

## Context
Initialize the database with the new `Category` table and seed it with common categories.

## Description
Update `RecordRepository._onCreate` to:
1. Create the `Category` table with `category_id (INTEGER PRIMARY KEY AUTOINCREMENT)`, `name (TEXT NOT NULL)`, and `type (TEXT NOT NULL)`.
2. Add a `category_id` column to the `record` table as a foreign key.
3. Seed the `Category` table with: Food, Transport, Entertainment, Salary, Rent, Health, and Uncategorized.
4. Ensure the `Uncategorized` category is inserted first or has a known ID for defaulting.

## Implementation Steps
- Step 1: Update `_onCreate` schema definition.
- Step 2: Add `INSERT INTO Category` statements for the initial seed data.
- Step 3: Verify the foreign key constraint from `record` to `Category`.

## Acceptance Criteria
- [ ] **FR-1 / Schema:** `Category` table exists after a fresh database init.
- [ ] **FR-2 / Seeding:** 7 categories are present in the table by default.

## Verification
- Run the app or a DB test to confirm tables are created and seeded.
