---
name: Create MonthDivider Component
status: closed
created: 2026-03-21T00:00:00Z
updated: 2026-03-21T00:00:00Z
complexity: simple
recommended_model: sonnet
phase: 2
priority: P1
github: "https://github.com/dainn67/wallet-ai/issues/78"
depends_on: [001]
parallel: true
conflicts_with: []
files:
  - lib/components/month_divider.dart
prd_requirements:
  - FR-2
---

# Task: Create MonthDivider Component

## Context
A reusable component is needed to display month/year headers (e.g., "March 2026") that separate groups of records in the `RecordsTab`.

## Description
Implement the `MonthDivider` widget in `lib/components/month_divider.dart`. This component should accept a `monthYear` string and display it in a clean, subtle style that visually segments the list of records.

## Acceptance Criteria
- [x] **FR-2 / UI Component:** A new stateless widget `MonthDivider` exists.
- [x] **FR-2 / Styling:** Divider displays text (e.g., "March 2026") with appropriate padding and subtle typography.
- [x] **FR-2 / Format:** Month name and year are correctly displayed.

## Implementation Steps

### Step 1: Create MonthDivider Widget
- Create `lib/components/month_divider.dart`.
- Implement `MonthDivider` extending `StatelessWidget`.
- Parameters: `required String label`.
- Add a Barrel export in `lib/components/components.dart` if applicable.

### Step 2: Implement Layout & Style
- Use `Padding` around the text.
- Use `GoogleFonts.poppins(fontSize: 12, fontWeight: FontWeight.w600, color: const Color(0xFF64748B))`.
- Optionally include a subtle line (`Divider`) or a colored background for the header.

## Technical Details
- **Approach:** Follow the existing "component" pattern.
- **Files to create:**
  - `lib/components/month_divider.dart`.
- **Patterns to follow:** Consistent typography and spacing with the rest of the app.
- **Edge cases:** Handling long month names.

## Tests to Write
### Unit Tests
- `test/components/month_divider_test.dart`
  - Test: Widget renders given label text.
  - Test: Layout and styling match design specifications.

## Verification Checklist
- [x] Unit tests pass: `fvm flutter test test/components/month_divider_test.dart`.
- [x] Barrel export check: Verify it can be imported from `package:wallet_ai/components/components.dart`.

## Dependencies
- **Blocked by:** 001
- **Blocks:** 020
- **External:** None.
