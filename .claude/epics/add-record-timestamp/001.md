---
name: Verify Model & Add Dependency
status: closed
created: 2026-03-21T00:00:00Z
updated: 2026-03-21T10:00:00Z
complexity: simple
recommended_model: sonnet
phase: 1
priority: P0
github: "https://github.com/dainn67/wallet-ai/issues/76"
depends_on: []
parallel: false
conflicts_with: []
files:
  - pubspec.yaml
  - lib/models/record.dart
  - lib/repositories/record_repository.dart
prd_requirements:
  - FR-3
---

# Task: Verify Model & Add Dependency

## Context
This task establishes the foundation for date formatting by adding the `intl` package and ensuring that the `Record` model consistently treats `createdAt` as milliseconds since epoch.

## Description
Add the `intl` library to `pubspec.yaml` and run `fvm flutter pub get`. Audit the `Record` model and `RecordRepository` to confirm that all records created (via AI or manually) have a valid `createdAt` timestamp in milliseconds since epoch, which is required for accurate sorting and formatting in later tasks.

## Acceptance Criteria
- [x] **FR-3 / Happy path:** `intl` package is added to `pubspec.yaml` and successfully fetched.
- [x] **FR-3 / Model logic:** `Record` constructor ensures `createdAt` is populated if null.
- [x] **FR-3 / Persistence:** `RecordRepository` correctly saves and retrieves `createdAt` as an integer (milliseconds).

## Implementation Steps

### Step 1: Add `intl` dependency [COMPLETED]
- Modify `pubspec.yaml` to include `intl: ^0.19.0` under `dependencies`.
- Run: `fvm flutter pub get`.

### Step 2: Audit Record Model [COMPLETED]
- Open `lib/models/record.dart`.
- Verify the constructor: `createdAt = createdAt ?? recordId ?? DateTime.now().millisecondsSinceEpoch;`.
- Ensure `fromMap` and `toMap` handle `created_at` correctly as an `int`.

### Step 3: Audit Record Repository [COMPLETED]
- Open `lib/repositories/record_repository.dart`.
- Confirm the `record` table schema has `created_at INTEGER NOT NULL`.
- Check `_onUpgrade` logic for version 3 to ensure legacy records have a valid timestamp.

## Technical Details
- **Approach:** Follow the existing singleton and model patterns.
- **Files to create/modify:**
  - `pubspec.yaml`: Add `intl` dependency.
  - `lib/models/record.dart`: Verify constructor and map methods.
  - `lib/repositories/record_repository.dart`: Verify schema and migration logic.
- **Patterns to follow:** Standard Flutter dependency management and SQFlite integration.
- **Edge cases:** Handle legacy records with null `created_at` in migration (already partially implemented in `RecordRepository`).

## Tests to Write
### Unit Tests
- `test/models/record_test.dart`
  - Test: `Record` creation with null `createdAt` → expect current timestamp. [COMPLETED]
  - Test: `Record.fromMap` with timestamp → expect correct value retrieved. [COMPLETED]

## Verification Checklist
- [x] Dependency check: `fvm flutter pub get` succeeds.
- [x] Unit tests pass: `fvm flutter test test/models/record_test.dart`.
- [ ] Database schema check (manual): Verify `created_at` column exists in SQLite if possible.

## Dependencies
- **Blocked by:** None
- **Blocks:** T010, T011
- **External:** `intl` package from pub.dev.
