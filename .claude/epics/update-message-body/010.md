---
name: Integrate Providers
status: closed
phase: 2
priority: P1
complexity: simple
recommended_model: sonnet
depends_on: [002]
prd_requirements: [FR-3]
files: [lib/providers/chat_provider.dart]
---

# Task: Integrate Providers

## Context
Bridge the record state and the chat state.

## Description
Update `ChatProvider` to hold a reference to `RecordProvider`. In `sendMessage`, fetch the formatted context strings using the new `ChatApiService` helpers and the `RecordProvider` data, then pass them to `streamChat`.

## Implementation Steps
- Step 1: Add `RecordProvider? _recordProvider` to `ChatProvider`.
- Step 2: Add a setter or constructor parameter for `recordProvider`. (Note: Manage this via `ProxyProvider` in `main.dart` if needed).
- Step 3: In `sendMessage`, generate `categoryList` and `moneySourceList` strings before the API call.
- Step 4: Pass these strings to the `ChatApiService().streamChat` call.

## Acceptance Criteria
- [ ] **FR-3 / Integration:** `ChatProvider` correctly passes app context to the service.
- [ ] **FR-3 / Stability:** No circular dependencies or crashes during initialization.

## Verification
- Verify data flow in the debugger.
