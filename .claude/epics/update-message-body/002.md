---
name: Update streamChat Interface
status: closed
updated: 2026-03-19
phase: 1
priority: P1
complexity: simple
recommended_model: sonnet
depends_on: [001]
prd_requirements: [FR-2]
files: [lib/services/chat_api_service.dart]
---

# Task: Update streamChat Interface

## Context
Expose context parameters in the chat service API.

## Description
Modify `ChatApiService.streamChat` to accept `categoryList` and `moneySourceList` strings. Inject these into the `inputs` field of the Dify API request payload.

## Implementation Steps
- Step 1: Update `streamChat` function signature to include the two new optional parameters.
- Step 2: Update the `inputs` map in the request body to include `category_list` and `money_source_list` if they are not empty.

## Acceptance Criteria
- [ ] **FR-2 / API Request:** The outgoing JSON payload to Dify contains the context strings.
- [ ] **FR-2 / Backwards Compatibility:** The function still works if context strings are null/empty.

## Verification
- Inspect the network payload using a debugger or logging.
