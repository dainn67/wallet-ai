---
name: Extract Test Tab
status: open
phase: 2
priority: P1
complexity: simple
recommended_model: sonnet
depends_on: [001]
parallel: true
prd_requirements: [FR-2]
files: [lib/screens/home/tabs/test_tab.dart]
---

# Task: Extract Test Tab

## Context
Move the developer test UI and demo data generation logic from `chat_screen.dart` to a dedicated `TestTab` widget.

## Description
Create `lib/screens/home/tabs/test_tab.dart`. This should contain the `TestTab` widget (extracted from `_TestTabContent`) and its associated private helper methods for adding demo records and money sources.

## Implementation Steps
- Step 1: Create `lib/screens/home/tabs/test_tab.dart`.
- Step 2: Implement `TestTab` as a `StatelessWidget`.
- Step 3: Copy the build logic and private methods (`_addDemoRecords`, `_addDemoMoneySources`) from `_TestTabContent`.
- Step 4: Ensure all necessary imports are added, including models and providers.
- Step 5: Verify that the buttons correctly trigger demo data addition.

## Acceptance Criteria
- [ ] **FR-2 / Happy path:** Buttons to add demo records and money sources work as expected.
- [ ] **NFR-2 / Logic Parity:** Demo data generation logic remains unchanged.

## Interface Contract
- Receives: `HomeScreen` layout via `TabBarView`.
- Produces: `TestTab` widget for `HomeScreen`.

## Verification
- Verify the `TestTab` works correctly once integrated.
