---
prd: ai-pattern-analyze
date: 2026-04-01T06:57:55Z
status: warning
score: 24/28 checks passed
---

# Validation Report: ai-pattern-analyze

## Summary
**Status:** WARNING
**Score:** 24/28 (completeness: 12/12, correctness: 7/8, coherence: 5/8)
**Scale:** small

## Critical Issues (must fix before prd-parse)

None.

## Warnings (should fix)

- [ ] **[Correctness]:** Dependencies section still references `getCategoryTotals(start, end)` (line 183) — leftover from pre-edit. Constraint was updated to remove this reference but Dependencies was not. Creates reader confusion. — Section: Dependencies — Fix: Remove `getCategoryTotals(start, end)` from the `RecordRepository` dependency description. Keep only `getAllRecords()`.

- [ ] **[Correctness]:** FR-6 specifies `by_category` as "expense only" but leaves `by_time_of_day` and `by_money_source` ambiguous — are income records included? The scenario only tests expense records. If income records appear in those two aggregations but not in `by_category`, AI server gets inconsistent totals. — Section: FR-6 — Fix: Add explicit qualifier: "expense only" to all three aggregation maps, OR state income is included in `by_time_of_day` and `by_money_source`.

- [ ] **[Coherence]:** NFR-1 threshold ("500ms for up to 500 records") only accounts for records within the 90-day window. But `getAllRecords()` fetches ALL records from DB (no date filter). A user with 2000 total records (300 in last 90 days) still loads 2000 rows, filter happens in memory. NFR-1 may be violated for power users. — Section: NFR-1 — Fix: Either (a) update NFR-1 to clarify it measures against total DB size, not window size, e.g. "500ms for total DB ≤ 2000 records", OR (b) acknowledge in Assumptions that performance degrades linearly with total record count.

- [ ] **[Correctness]:** Scale declared as `small` but PRD has 7 FR + 1 NTH + 3 NFR = 11 requirements. Per prd-quality.md, SMALL = 1-3 tasks. A 7-FR PRD typically maps to 3-5 tasks (MEDIUM range). Scale mismatch may cause `prd-parse` to under-allocate task breakdown. — Section: Frontmatter — Fix: Consider changing `scale: small` to `scale: medium`, OR acknowledge that all 7 FRs map to a single implementation task (1 file, ~150 lines) and annotate accordingly in `_Metadata`.

## Passed Checks

- [x] All required sections present for SMALL scale
- [x] Frontmatter complete: name, description, status, priority, scale, created, updated
- [x] Requirement IDs sequential and unique: FR-1..7, NTH-1, NFR-1..3
- [x] Every FR has ≥1 GIVEN/WHEN/THEN scenario (FR-2, FR-3, FR-4, FR-5 have 2 scenarios each)
- [x] NTH-1 has scenario
- [x] NTH-1 return type fixed: `Future<String>` ✅
- [x] FR-4 and FR-5 now use explicit rolling window syntax (`DateTime.now().subtract(...)`)
- [x] FR-4 and FR-5 specify empty-window behavior (`records: [], totals: 0`)
- [x] FR-2 `date` field explicitly uses LOCAL date `yyyy-MM-dd`
- [x] Constraint updated: references `getAllRecords()` with explanation of JOIN-based name resolution
- [x] Executive Summary: what, who, why, why now — all present
- [x] Problem Statement appropriate for developer-consumer PRD
- [x] Success Criteria measurable with method (unit test)
- [x] Risk: 1 entry with severity + likelihood + mitigation ✅ (correct for SMALL)
- [x] Assumptions all state consequence if wrong
- [x] Out of Scope explicit with follow-up PRD references
- [x] No Out-of-Scope items appearing in Requirements
- [x] FR-3 bucket boundaries defined (hour ranges) and tested (23:30, 11:00 boundary)
- [x] NFR thresholds measurable (500ms/500 records, zero BuildContext)
- [x] _Metadata requirement_ids match actual IDs in document
- [x] No duplicate requirements
- [x] No ambiguous unmeasured terms
- [x] Dependencies have owner and status
- [x] FR-6 scenario concrete with named values

## Recommendations

1. **FR-1 scenario is structural** — tests singleton pattern, not meaningful behavior. Replace with failure scenario (uninitialized repository) to catch the assumption stated in Constraints.
2. **Add `record_count` to summary block** — trivial to implement, gives server a quick sanity check without parsing the full records array.
3. **Consider documenting the full snapshot JSON shape** in an appendix — having a complete example makes the server contract unambiguous and doubles as a test fixture.
