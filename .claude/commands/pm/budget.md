---
model: haiku
name: budget
description: Token budget estimate for a feature workflow (0 LLM tokens)
allowed-tools: Bash
---

Run this command and display the output:
```bash
bash .claude/scripts/pm/budget.sh "$1"
```
